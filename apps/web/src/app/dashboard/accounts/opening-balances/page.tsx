'use client';import{useEffect,useState}from'react';import{Loader2,RefreshCw}from'lucide-react';import{apiClient}from'../../../../../lib/api-client';export default function Opening(){const[b,setB]=useState<any[]>([]);const[l,setL]=useState(true);const[e,setE]=useState('');const load=async()=>{setL(true);try{setB(await apiClient.get('/accounting/opening-balances'));setE('')}catch(x:any){setE(x?.message||'Unable to load opening balances')}finally{setL(false)}};useEffect(()=>{load()},[]);return <div className="mx-auto max-w-6xl space-y-4 p-2 sm:p-4"><div className="flex justify-between rounded-2xl bg-[#4D5E45] p-6 text-white"><div><h1 className="text-2xl font-bold">Opening Balance Batches</h1><p className="mt-1 text-sm text-green-100">Migration-control register. Validation and posting remain finance-controlled.</p></div><button onClick={load}>{l?<Loader2 className="animate-spin"/>:<RefreshCw/>}</button></div>{e&&<div className="rounded bg-red-50 p-3 text-red-700">{e}</div>}<div className="rounded-xl border bg-white p-4">{l?<Loader2 className="animate-spin"/>:b.length?<pre className="overflow-auto text-xs">{JSON.stringify(b,null,2)}</pre>:<div className="p-6 text-sm text-slate-500">No opening-balance batches are loaded. Finance must provide signed-off opening balances before validation and posting.</div>}</div></div>}
