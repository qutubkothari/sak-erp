'use client';import{useEffect,useState}from'react';import{Loader2,RefreshCw}from'lucide-react';import{apiClient}from'../../../../../lib/api-client';export default function Schedules(){const[s,setS]=useState<any[]>([]);const[l,setL]=useState(true);const[e,setE]=useState('');const load=async()=>{setL(true);try{setS(await apiClient.get('/accounting/report-schedules'));setE('')}catch(x:any){setE(x?.message||'Unable to load report schedules')}finally{setL(false)}};useEffect(()=>{load()},[]);return <div className="mx-auto max-w-6xl space-y-4 p-2 sm:p-4"><div className="flex justify-between rounded-2xl bg-[#3D526D] p-6 text-white"><div><h1 className="text-2xl font-bold">Report Schedules</h1><p className="mt-1 text-sm text-blue-100">Configured financial-report schedules. No report is generated or delivered from this screen.</p></div><button onClick={load}>{l?<Loader2 className="animate-spin"/>:<RefreshCw/>}</button></div>{e&&<div className="rounded bg-red-50 p-3 text-red-700">{e}</div>}<div className="rounded-xl border bg-white p-4">{l?<Loader2 className="animate-spin"/>:s.length?<pre className="overflow-auto text-xs">{JSON.stringify(s,null,2)}</pre>:<div className="p-6 text-sm text-slate-500">No report schedules are configured. Finance must approve recipients, cadence, and report definitions.</div>}</div></div>}
