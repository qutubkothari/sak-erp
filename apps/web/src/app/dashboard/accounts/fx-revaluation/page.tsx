'use client';import{useEffect,useState}from'react';import{Loader2,RefreshCw}from'lucide-react';import{apiClient}from'../../../../../lib/api-client';export default function Fx(){const[d,setD]=useState<any>(null);const[l,setL]=useState(true);const[e,setE]=useState('');const load=async()=>{setL(true);try{setD(await apiClient.get('/accounting/fx-revaluation/preview'));setE('')}catch(x:any){setE(x?.message||'Unable to load FX revaluation preview')}finally{setL(false)}};useEffect(()=>{load()},[]);return <div className="mx-auto max-w-6xl space-y-4 p-2 sm:p-4"><div className="flex justify-between rounded-2xl bg-[#304A68] p-6 text-white"><div><h1 className="text-2xl font-bold">FX Revaluation Preview</h1><p className="mt-1 text-sm text-blue-100">Read-only unrealised FX preview. No valuation adjustment or journal is posted.</p></div><button onClick={load}>{l?<Loader2 className="animate-spin"/>:<RefreshCw/>}</button></div>{e&&<div className="rounded bg-red-50 p-3 text-red-700">{e}</div>}<div className="rounded-xl border bg-white p-6 text-sm text-slate-600">{l?<Loader2 className="animate-spin"/>:d?<pre className="overflow-auto text-xs">{JSON.stringify(d,null,2)}</pre>:'No preview data is available. Finance must load approved exchange rates and foreign-currency balances.'}</div></div>}
