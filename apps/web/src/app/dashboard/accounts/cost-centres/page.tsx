'use client';import{useEffect,useState}from'react';import{Loader2,RefreshCw}from'lucide-react';import{apiClient}from'../../../../../lib/api-client';type Centre={id:string;cost_centre_code?:string;cost_centre_name?:string;is_active?:boolean};export default function Centres(){const[c,setC]=useState<Centre[]>([]);const[l,setL]=useState(true);const[e,setE]=useState('');const load=async()=>{setL(true);try{setC(await apiClient.get<Centre[]>('/accounting/cost-centres'));setE('')}catch(x:any){setE(x?.message||'Unable to load cost centres')}finally{setL(false)}};useEffect(()=>{load()},[]);return <div className="mx-auto max-w-6xl space-y-4 p-2 sm:p-4"><div className="flex justify-between rounded-2xl bg-[#4B4A38] p-6 text-white"><div><h1 className="text-2xl font-bold">Cost Centres</h1><p className="mt-1 text-sm text-lime-100">Management-accounting structure. Finance must define the approved hierarchy before allocation reporting is used.</p></div><button onClick={load}>{l?<Loader2 className="animate-spin"/>:<RefreshCw/>}</button></div>{e&&<div className="rounded bg-red-50 p-3 text-red-700">{e}</div>}<div className="rounded-xl border bg-white p-4">{l?<Loader2 className="animate-spin"/>:c.length?<table className="min-w-full text-sm"><thead className="border-b text-left text-xs uppercase text-slate-500"><tr><th className="p-2">Code</th><th className="p-2">Cost Centre</th><th className="p-2">Status</th></tr></thead><tbody>{c.map(x=><tr key={x.id} className="border-b"><td className="p-2">{x.cost_centre_code||'—'}</td><td className="p-2 font-medium">{x.cost_centre_name||'Cost Centre'}</td><td className="p-2">{x.is_active===false?'Inactive':'Active'}</td></tr>)}</tbody></table>:<div className="p-6 text-sm text-slate-500">No cost centres are configured yet. Finance must supply the approved hierarchy.</div>}</div></div>}
