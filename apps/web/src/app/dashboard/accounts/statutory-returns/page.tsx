'use client';import{useEffect,useState}from'react';import{Loader2,RefreshCw}from'lucide-react';import{apiClient}from'../../../../../lib/api-client';type Return={id:string;return_type?:string;period_from?:string;period_to?:string;due_date?:string;status?:string};export default function Returns(){const[r,setR]=useState<Return[]>([]);const[l,setL]=useState(true);const[e,setE]=useState('');const load=async()=>{setL(true);try{setR(await apiClient.get<Return[]>('/accounting/statutory-returns'));setE('')}catch(x:any){setE(x?.message||'Unable to load statutory returns')}finally{setL(false)}};useEffect(()=>{load()},[]);return <div className="mx-auto max-w-6xl space-y-4 p-2 sm:p-4"><div className="flex justify-between rounded-2xl bg-[#633B3B] p-6 text-white"><div><h1 className="text-2xl font-bold">Statutory Returns</h1><p className="mt-1 text-sm text-rose-100">Return register for finance review. This screen does not generate, submit, or file a statutory return.</p></div><button onClick={load}>{l?<Loader2 className="animate-spin"/>:<RefreshCw/>}</button></div>{e&&<div className="rounded bg-red-50 p-3 text-red-700">{e}</div>}<div className="rounded-xl border bg-white p-4">{l?<Loader2 className="animate-spin"/>:r.length?<table className="min-w-full text-sm"><thead className="border-b text-left text-xs uppercase text-slate-500"><tr><th className="p-2">Return</th><th className="p-2">Period</th><th className="p-2">Due</th><th className="p-2">Status</th></tr></thead><tbody>{r.map(x=><tr key={x.id} className="border-b"><td className="p-2 font-medium">{x.return_type||'Return'}</td><td className="p-2">{x.period_from||'—'} – {x.period_to||'—'}</td><td className="p-2">{x.due_date||'—'}</td><td className="p-2"><span className="rounded bg-slate-100 px-2 py-1 text-xs">{x.status||'DRAFT'}</span></td></tr>)}</tbody></table>:<div className="p-6 text-sm text-slate-500">No statutory returns are configured. Finance must validate the company’s actual filing requirements.</div>}</div></div>}
