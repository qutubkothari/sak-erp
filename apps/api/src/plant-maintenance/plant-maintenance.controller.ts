import { Body, Controller, Get, Patch, Post, Req, Param, UseGuards } from '@nestjs/common';
import { JwtAuthGuard } from '../auth/guards/jwt-auth.guard'; import { PermissionsGuard } from '../auth/guards/permissions.guard'; import { PlantMaintenanceService } from './plant-maintenance.service';
@Controller('plant-maintenance') @UseGuards(JwtAuthGuard, PermissionsGuard)
export class PlantMaintenanceController { constructor(private readonly service: PlantMaintenanceService) {} @Get('assets') assets(@Req() req:any){return this.service.assets(req.user.tenantId)} @Post('assets') asset(@Req() req:any,@Body() body:any){return this.service.createAsset(req.user.tenantId,body)} @Get('work-orders') workOrders(@Req() req:any){return this.service.workOrders(req.user.tenantId)} @Post('work-orders') workOrder(@Req() req:any,@Body() body:any){return this.service.createWorkOrder(req.user.tenantId,req.user.userId,body)} @Patch('work-orders/:id') update(@Req() req:any,@Param('id') id:string,@Body() body:any){return this.service.updateWorkOrder(req.user.tenantId,id,body)} }
